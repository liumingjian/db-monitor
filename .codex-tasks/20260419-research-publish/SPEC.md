# Task

Create a public GitHub repository named `db-monitor` for the current workspace,
capture the completed architecture and product research into `RESEARCH.md`, and
cache the relevant findings inside this repository.

# Deliverables

1. A complete `RESEARCH.md` at the repository root.
2. Task tracking artifacts for recovery.
3. A local Git repository connected to a public GitHub repository.
4. A committed and pushed initial state.

# Constraints

- Keep the research grounded in inspected project files and confirmed decisions.
- Do not publish deployment-only secrets or ignored local config files.
- Keep the public repository content focused on the project and research.

# Validation

- `RESEARCH.md` exists and is populated.
- `git status --short --branch` is clean after commit.
- `gh repo view` succeeds for the created public repository.

